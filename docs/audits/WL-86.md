# Auditor Digest — WL-86

**티켓**: WL-86 — 신규 파트너 생성 시 contents 기본 row 자동 생성 트리거 구현  
**트랙**: 🚨 HIGH  
**완료일**: 2026-04-15  
**Human Check 승인**: 문경 님 (2026-04-15)

---

### 📝 Core Logic

`partners` 테이블 `AFTER INSERT` 트리거(`trg_init_partner_defaults`)가 `fn_init_partner_defaults()` 함수를 호출하여 신규 파트너에 대해 `partner_sections` 8행 + `contents` 11행(마케팅 8 + 법적 고지 3)을 자동 생성한다. 모든 INSERT는 `ON CONFLICT DO NOTHING`으로 멱등성 보장.

---

### Identified Risks

| # | 위험 | 완화 |
|---|------|------|
| 1 | SECURITY DEFINER 권한 범위 | `SET search_path = public` + `REVOKE EXECUTE FROM PUBLIC` + 외부 입력 없음(NEW.id UUID만 사용) |
| 2 | 트리거 오류 시 파트너 INSERT 롤백 | Visible Fail 정책 채택 — Silent Fail 배제. `partner_sections` CHECK 제약과 INSERT 데이터 완전 일치 확인으로 실제 오류 가능성 최소화 |

---

### Human Check 승인 포인트

1. SECURITY DEFINER 사용 동의 (`trg_sync_domain_to_partner`와 동일 패턴)
2. 트리거 오류 시 파트너 INSERT 롤백 허용 (Silent Fail 배제 결정)
3. 신규 마이그레이션 파일 `20260415000002_add_partner_init_trigger.sql` SQL Editor 직접 실행 (문경 님 담당)

---

### 실행 파일

`supabase/migrations/20260415000002_add_partner_init_trigger.sql`

### 검증 체크리스트 (SQL Editor 실행 후)

- [ ] `partner_sections` 8개 row 자동 생성 확인 (is_visible = true)
- [ ] `contents` 마케팅 8개 row 자동 생성 확인 (is_published = true)
- [ ] `contents` 법적 고지 3개 row 자동 생성 확인 (is_published = false)
- [ ] 기존 파트너 데이터 영향 없음 확인
- [ ] 테스트 파트너 DELETE 후 CASCADE 정리 확인
