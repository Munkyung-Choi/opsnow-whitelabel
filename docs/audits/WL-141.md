---
ticket: WL-141
track: Critical
date: 2026-04-20
---

# WL-141 Auditor Digest — cleanup_e2e_test_users RPC

## Core Logic

1. `auth.users`에서 E2E 고정 이메일 2건(`master-admin-e2e@test.local`, `partner-admin-e2e@test.local`)으로 ID 수집 (소프트 삭제 유저 포함)
2. FK 체인 역순 정리: `global_contents.updated_by` SET NULL → `system_logs.actor_id` DELETE → `partners.owner_id` DELETE → `profiles.id` DELETE
3. `auth.users` hard-delete로 GoTrue Admin API 소프트 삭제 사각지대 우회
4. `SECURITY DEFINER` + `SET search_path = public, pg_temp` + `service_role` EXECUTE 전용 GRANT

## Identified Risks

**없음.** Security Auditor sub-agent 교차검증 결과 실제 위험 요소 미발견.

- 권한 격상: `authenticated`/`anon` REVOKE + `service_role` GRANT → 브라우저/클라이언트 경로 호출 불가
- Search path: `public, pg_temp` 순서는 Supabase 권장 패턴과 일치
- FK 체인 완전성: `grep "REFERENCES auth.users"` 전수 조사 4개 FK 모두 RPC에 포함. `contents`/`leads`/`site_visits`/`partner_sections`/`domain_requests`는 `partners.id ON DELETE CASCADE`로 자동 정리
- 이메일 하드코딩: `TEST_ADMIN_CREDENTIALS`(`tests/fixtures/seed-admin-users.ts:5,9`)와 정확히 일치. production 오염 가능성 없음
- 트랜잭션 무결성: plpgsql 함수 단일 TX → 부분 커밋 위험 없음

## Human Check 승인 포인트 (2026-04-20 승인완료)

1. **설계안 수용**: RPC 구조(FK 체인 + SECURITY DEFINER + service_role) 전면 승인
2. **로컬 선검증**: 로컬 DB migration up + RPC 실행(반환 0) + 권한 grant 확인 → cloud push 순서로 집행
3. **헤더 주석**: `@sync_target: tests/fixtures/seed-admin-users.ts` 경로 포함하여 추적성 확보

## 외부 LLM 피드백 판정 (`/aisend` 2회 적용)

| 주장 | 판정 | 비고 |
|------|------|------|
| Soft-delete Trap 현상 진단 | ✅ 정확 | CI 보고서 7~11 실증 |
| FK 참조 무결성으로 deleteUser 지연 | ✅ 정확 | system_logs/global_contents RESTRICT 확인 |
| `app.settings.environment` 환경 체크 권고 | ❌ 오진 | 프로젝트에 해당 GUC 미존재 — 제거 |
| `@sync_target: tests/e2e/fixtures/...` 경로 | ❌ 오진 | 실제 경로 `tests/fixtures/...`로 수정 |
| RPC 구조의 FK 체인 완전성 | ✅ 정확 | 내부 Auditor 검증 일치 |

## Verify 결과

- lint: 0 errors (pre-existing warning 1건)
- tsc: 0 errors
- Vitest: 309 passed / 1 todo
- Playwright: 205 passed / 5 skipped(기존 todo) / 0 failed

## 수정 파일

- 신규: `supabase/migrations/20260420000002_add_cleanup_e2e_test_users_rpc.sql`
- 수정: `tests/fixtures/seed-admin-users.ts` (-49 / +11)
- 수정: `src/types/supabase.ts` (RPC 타입 추가)
