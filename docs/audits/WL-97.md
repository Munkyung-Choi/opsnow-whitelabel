# Auditor Digest — WL-97

**작성일**: 2026-04-16  
**트랙**: HIGH  
**Human Check 승인**: 2026-04-16 (문경 님 대리인 승인)

---

### 📝 Core Logic

`global_contents.meta` JSONB에 `categories + items` 구조 시딩 (SQL UPSERT).  
`fetchFaqMeta()` React `cache()`로 DB 쿼리 1회 제한.  
`getFaqHub` → answer 제외 경량화, `getFaqDetailBySlug` → slug 기준 find() + JSON-LD 생성.

---

### 🔍 Identified Risks & 대응

| # | 위험 | 대응 |
|---|------|------|
| R-1 | UPSERT로 기존 faq row 덮어쓰기 | 시딩 SQL에 롤백 쿼리 주석 포함. `parseFaqContent` 하위호환 패치 (summary 부재 시 answer 파생) |
| R-2 | slug 유일성 DB 레벨 미보장 | 시딩 SQL 상단에 경고 주석 + flat slug 포맷 가이드. `find()` 첫 번째 일치 반환 |
| R-3 | Markdown answer → SEO seoDescription 노출 | `stripMarkdown()`: `[text](url)` → text 유지·URL 제거, bold/heading/code 모두 strip |
| R-4 | `isFeatured` 플래그 — 기존 `parseFaqContent`에 없는 개념 | WL-96 scope 위임. WL-97은 파싱만 준비 |

---

### ✋ Human Check 승인 포인트

1. **설계 방향**: UPSERT + 하위호환 패치 전략 승인
2. **SQL 실행 주체**: 시딩 SQL은 문경 님이 Supabase SQL Editor에서 직접 실행 (`supabase/snippets/seed_faq_global_content.sql`)
3. **API 범위**: `getFaqHub` + `getFaqDetailBySlug` — WL-96 UI 구현을 위한 인프라로 충분함 승인

---

### 📂 변경 파일

| 파일 | 유형 |
|------|------|
| `supabase/snippets/seed_faq_global_content.sql` | 신규 — 문경 님 직접 실행 |
| `src/lib/faq/get-faq-data.ts` | 신규 |
| `src/lib/faq/faq-data.ts` | 수정 (하위호환 패치) |
| `src/lib/faq/__tests__/faq-utils.test.ts` | 신규 (테스트 16개) |
