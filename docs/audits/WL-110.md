# Auditor Digest — WL-110

**작업일**: 2026-04-17  
**트랙**: 🚨 HIGH  
**Human Check**: 승인완료 (AP Architect via 문경 님, 2026-04-17)

---

### 📝 Core Logic

`contents.section_type` 컬럼에 CHECK 제약 조건 추가(13종).
`src/types/section-type.ts` SSOT와 동기화: Marketing(8) + Legacy(2) + Legal(3).
멱등성 보장(IF NOT EXISTS) + 사전 검증 블록(위반 데이터 존재 시 RAISE EXCEPTION).

---

### 🔍 Identified Risks (1건 → 수정)

| # | 심각도 | 위험 | 조치 |
|---|---|---|---|
| 1 | HIGH | 초기 마이그레이션 SQL에 잘못된 값 4건 — `legacy_features`, `legacy_integrations`, `terms_of_service`, `privacy_policy` 사용. 실행 시 법적 공지 페이지(terms, privacy) INSERT/UPDATE 차단 | seeds + SSOT 대조로 실제값 확인 후 수정: `footer`, `terms`, `privacy` 사용 |

---

### ✋ Human Check 승인 포인트

- 13종 확정 리스트(seeds + section-type.ts 대조 결과)로 수정된 안 승인
- `NOT VALID` 2단계 적용 미채택 — 데이터 규모 대비 복잡도 과잉, 단순 적용이 최적

---

### ⚠️ 후속 작업

- 신규 section_type 추가 시 `section-type.ts` + 마이그레이션 CHECK 제약 조건 동시 업데이트 필요 (SSOT 정합성)
