# Decision Journal: 2026-04-19

## Decisions

- **`/start-dev` ↔ `/end-dev` 대칭 세션 루프 확정** — 시작/종료가 짝을 이루면 맥락 복원과 박제가 자연스러운 루틴이 된다. 기존 `/journal` 단독 구조보다 역할 의도가 명확해짐. `/start-dev` STEP 5에 월요일 주간 리포트 자동 생성 탑재.

- **주간 롤업 트리거를 `/end-dev`(일/월)에서 `/start-dev`(월)로 이동** — 일요일 세션이 없으면 주간 리포트가 완전히 누락되는 구조적 결함 수정. 월요일 맥락 복원과 주간 리포트를 하나의 루틴으로 묶음.

- **AGENTS.md 경량화 + `docs/skills-guide.md` 분리** — AGENTS.md는 매 세션 자동 로드되어 토큰 비용에 직결. 스킬 참조 문서는 필요할 때만 여는 별도 파일(`docs/skills-guide.md`)로 격리. AP Architect 피드백에서 유일하게 채택한 원칙인 Cleanup 선행 커밋도 CLAUDE.md §11.4에 명문화.

## Rejected Paths

- **AP Architect의 LaTeX 마이그레이션 비용 공식** — 스타트업 MVP 규모에서 ALTER TABLE lock은 실질 위험 아님. 수식이 결정을 좌우하면 안 된다.
- **Data Contract 4섹션 제안 (AP 피드백)** — `docs/journal/_template.md`와 동일 구조. 이미 존재하는 것을 다시 제안한 중복 의견으로 기각.
- **"Weekly Confluence = AI 장기 기억" 프레임** — 기술적으로 부정확. Claude는 로컬 파일 시스템을 직접 읽고, Confluence는 사람용 아카이브. 혼동 방지를 위해 거절.
- **일간 Confluence 발행** — Claude가 로컬 MD를 직접 grep할 수 있으므로 이중 발행 불필요. 사람이 읽는 단위는 주간이 적절.

## Debts & Risks

- `docs/skills-guide.md`와 AGENTS.md 스킬 테이블 간 일부 중복 존재. 현 시점에서는 허용하되, AGENTS.md 스킬 테이블이 너무 세분화되면 통합 정리 필요.
- Confluence 03 내장 커맨드 List (309264441)는 전체 드롭다운 캡처 기반 검증 전까지 draft 상태. 공식 참조 문서로 쓰기 전 재확인 필요.

## Message to Tomorrow

- 커밋 미완료 파일 존재: `.claude/skills/end-dev/`(신규), `AGENTS.md`, `CLAUDE.md`, `docs/skills-guide.md` — 세션 시작 전 커밋 처리 권고.
- 다음 주요 작업 순서: `WL-118` Impl (RLS Triangular 테스트) → `WL-121`/`WL-122` (RLS 정책 보완) → `WL-119` withAdminAction v2 (Critical, 동기 Human Check 필요).
- 4-13~4-19 주간 리포트(Confluence 309231650)에 오늘 일지 반영 미완 상태. 각주를 `docs/journal/2026-04-18.md` 및 `docs/journal/2026-04-19.md` 양쪽으로 업데이트 필요.
